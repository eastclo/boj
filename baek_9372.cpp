#include <bits/stdc++.h>

using namespace std;

const int MN = 1001;
const int INF = 1e9;
int N, M;

int main(void)
{
	ios::sync_with_stdio(false);	cin.tie(NULL);
	int T;	cin >> T;
	while(T--) {
		cin >> N >> M;
		for(int i = 0; i < M; i++) {
			int a, b;	cin >> a >> b;
		}

		cout << N-1 << '\n';
	}
}
