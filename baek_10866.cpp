#include <bits/stdc++.h>

using namespace std;
#define ll long long

const int MN = 16;
const ll INF = 1e18;

ll arr[MN], N, K;
int main(void)
{
	ios::sync_with_stdio(false);	cin.tie(NULL);
	cin >> N;
	for(int i = 0; i < N; i++) 
		cin >> arr[i];
	cin >> K;


}
